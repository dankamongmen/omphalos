---
kind: pipeline
type: docker
name: debian-unstable

steps:
- name: debian-build
  image: dankamongmen/unstable_builder:2020-10-10a
  commands:
    - export LANG=en_US.UTF-8
    - apt-get -y update
    - apt-get -y upgrade
    - autoreconf -fis
    - ./configure
    - make
    - make test
